# web-web

Web 站点/页面渲染相关前端应用路径：`fast-ui/apps/web-web`

## 特点

- 包含 `src/components/page/**`：页面渲染相关组件
- 与后端 `page-*` 领域（页面配置、组件库、发布管理）联动较多

## 启动

在 `fast-ui` 目录：

```bash
pnpm --filter web-web dev
```
